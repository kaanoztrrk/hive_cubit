name: hive_cubit
description: "A lightweight HiveCubit that auto-persists Cubit state to a Hive box with zero boilerplate."
version: 0.0.1
homepage: https://kaan.codes
repository: https://github.com/theviacoder/hive_cubit

environment:
  sdk: '>=3.0.0 <4.0.0'

dependencies:
  bloc: ^8.1.0
  hive: ^2.2.3

dev_dependencies:
  test: ^1.24.0
  lints: ^3.0.0
  hive_test: ^1.0.1
